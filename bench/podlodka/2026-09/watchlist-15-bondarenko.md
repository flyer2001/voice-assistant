# Лист ожидания — 15.09 20:00, Бондаренко, «AI как экзокостюм»

От myrep-39, 15.09 утро. ГЛАВНЫЙ доклад программы: автор оси harness vs
scaffold из HYP-049, тема = рамка возмещения Sergey.

## Приоритет 1 — ОРАКУЛ («оракул в приоритете» — прямая формулировка)

Вопрос: как агент понимает, что задача выполнена, БЕЗ человека.
Наш блокер (HYP-043), oracle weakness для iOS:
- swiftc: каскадные ошибки, один баг → 50
- UI-тесты flaky, как гейт ненадёжны
- корректность в рантайме — только ручной QA
- per-file state machine ломает cross-file контракты
- LLM-as-judge без измеренного precision/recall = «опасный самообман»

Сигналить: validation, evals, definition of done, проверка результата,
LLM-судьи. ОСОБО: цифры precision/recall судьи, если назовёт.
Отдельно: ложноположительные проверки — наш урок done_check ≠ «работает»
(тест отчитался успехом, увидев 3 компонента из 5).

## Приоритет 2 — ОРКЕСТРАЦИЯ (Sergey открыт, готов корректировать своё)

1. Нарезка задач на атомарные с машинно-проверяемыми критериями. У нас
   9-фазный workflow, спека = ground truth. Сигналить чужой способ резать
2. Heartbeat и бюджеты. У нас usage.jsonl, loop-detector, budget gate.
   Дыра: spawn-бюджеты были слепы. Открытый вопрос — лимит рекурсии на
   уровне harness, не гейтом перед запуском. Плюс: считает ли простой
   агентов, ждущих друг друга
3. Самообучаемость: инцидент → доработка → метрика → откат при регрессе.
   Как инцидент превращается в правило, руками или автоматом

Различение, подсказать при «дообучаем»: тюнинг весов (LoRA) ≠ обновление
инструкций на ходу. Sergey описывает второе.

НЕ сигналить: что такое harness, зачем скиллы, базовое про CLI-агентов.

Заметка myRep: «Podlodka AI - AI как экзокостюм...», там 10 готовых
вопросов по оракулу и оркестрации.
